# First create a new environment with conda (or any other package manager)
# conda create -n question_gen python=3.8 (other python versions should work as well)
# then activate the environment and
# pip install -r requirements_questions.txt
numpy==1.22.3
h5py==3.6.0
scipy==1.10.1
webcolors==24.8.0
Pillow==9.1.0
tqdm==4.67.1